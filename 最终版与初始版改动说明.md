# `main.py` 最终版与最初版改动说明

> 对比范围：
> - 最初版本：`43ea170`（首次包含 `main.py`/`chart.html` 的提交）
> - 最终版本：`ab3cef3`（当前最新提交）

---

## 1. 架构与依赖层面的主要变化

1. **检测主干从 YOLO 人体检测改为 Haar 人脸检测**
   - 最初版：用 `ultralytics.YOLO` 做 `person` 检测，再截上半身近似当人脸。
   - 最终版：改为 `cv2.CascadeClassifier(haarcascade_frontalface_default.xml)` 直接做人脸检测，避免“全身框推脸部”的误差传播。

2. **保留表情分类，但增强了推理设备选择**
   - 最初版：仅调用 `transformers.pipeline(...)`，未显式设备策略。
   - 最终版：支持自动检测 CUDA（有则 `device=0`，否则 `CPU`），并打印当前推理设备，便于排查性能问题。

---

## 2. 评分逻辑与时序平滑变化

1. **评分函数从“单标签映射”升级到“概率加权映射”**
   - 最初版：只取 top-1 情绪标签做分值映射。
   - 最终版：引入 `weighted_score_from_preds`，按各情绪概率加权计算，更稳健。

2. **新增时序平滑机制（EMA）**
   - 最终版加入 `ema_alpha` 与 `smoothed_score`，降低瞬时跳变对折线图的影响。

3. **增加低置信度过滤**
   - 通过 `min_face_conf` 剔除低可信预测，减少噪声框和误检对总分的污染。

---

## 3. 视频流与渲染机制变化（重点）

1. **从“跳帧+continue”改为“每帧渲染、间隔推理”**
   - 最初版跳过大量帧时会直接 `continue`，视觉上更容易“顿”。
   - 最终版每帧都走显示，检测/识别按间隔执行，同时复用上次检测框，流畅度更好。

2. **跨线程图像传输稳定性增强**
   - 最终版发送到 UI 的图像使用 `QImage(...).copy()` 深拷贝，避免底层 OpenCV/Numpy 缓冲区生命周期问题导致黑屏或随机退出。

3. **缩放策略调整**
   - 最终版在 UI 层按 `QLabel` 实际尺寸缩放，并加了控件尺寸为 0 时的保护逻辑，减少初始化阶段异常。

---

## 4. 稳定性与容错增强

1. **视频线程顶层异常保护**
   - 最终版在 `run()` 主循环加 `try/except` + `traceback`，线程报错不再静默失败，方便定位。

2. **视频源打开失败回退策略**
   - 最终版如果配置视频源打不开，会尝试回退摄像头 `0`；仍失败则明确打印日志并退出线程。

3. **视频源解析增强**
   - 支持：摄像头 ID、绝对路径、相对脚本路径，并对常见拼写问题做提示。

---

## 5. UI/可视化与可读性调整

1. **主窗口与布局比例优化**
   - 默认窗口增大到 `1600x900`，左侧视频占比提高（`7:3`），监控画面更大。

2. **叠加文本从中文切为英文可显示文本**
   - 由于 OpenCV 默认字体不支持中文，最终版统一使用 ASCII 文本，避免标签显示 `?????`。

3. **图表联动保留**
   - 仍通过 `QWebEngineView` 调用 `chart.html` 内的 `updateChart(...)` 做实时更新。

---

## 6. 行为差异总结（你实际能感知到的）

- **更少误检污染**：人脸尺寸/比例/置信度多重过滤。
- **更顺滑的画面**：渲染与推理解耦，减少“卡住一下再跳”的感觉。
- **更稳的程序**：黑屏、线程静默退出的概率显著降低；失败会有日志。
- **更易部署**：`VIDEO_SOURCE` 更灵活，路径错误时有回退和提示。

---

## 7. 备注

- 最终版仍是“轻量规则 + 通用表情模型”路线，适合快速部署验证。
- 若后续追求课堂远景高精度，可进一步升级为：
  - 更强人脸检测器（如 RetinaFace/SCRFD）
  - 人脸跟踪（ByteTrack/DeepSORT）
  - 结合头姿/视线等行为特征，而不只依赖表情。
